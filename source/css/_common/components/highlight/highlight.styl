// https://github.com/chriskempson/tomorrow-theme

@require "theme"

// Placeholder: $code-block
$code-block {
  overflow-x: auto;
  overflow-y: hidden;
  margin: 20px 0;
  padding: 8px 12px;
  font-size: $code-font-size;
  color: $highlight-foreground;
  line-height: $line-height-code-block;
}

pre, code { font-family: $code-font-family; }

code {
  padding: 1px 3px;
  margin: 0 3px;
  word-break: break-word;
  overflow: auto;
  border: 1px solid rgba(215, 215, 215, 0.6);
  color: $code-foreground;
  background: $code-background;
  border-radius: $code-border-radius;
  font-size: $code-font-size;
}

pre {
  @extend $code-block;
  background: transparent;

  code {
    padding: 0;
    color: $highlight-foreground;
    background: none;
    text-shadow: none;
    border: none;
  }
}

.highlight {
  @extend $code-block;

  border-radius: 3px;
  border: 1px solid rgba(221,221,221,.6);
  background: $highlight-background;

  pre {
    border: none;
    margin: 0;
    padding: 1px;
  }

  table {
    margin: 0;
    width: auto;
    border: none;
  }

  td {
    border: none;
    padding: 0;
  }

  figcaption {
    clearfix();
    font-size: 1em;
    color: $highlight-foreground;
    line-height: 1em;
    margin-bottom: 1em;

    a {
      float: right;
      color: $highlight-foreground;

      &:hover { border-bottom-color: $highlight-foreground; }
    }
  }

  .gutter pre {
    color: $grey-dim;
    text-align: right;
    padding-right: 20px;
  }

  .line { 
    height: 20px; 
    display: block;
  }
  .line.output {
    color: $highlight-output-color;
    padding-left: 15px;
  }
}

.highlight.plain {
  background: $highlight-plain-background;
}

.highlight.plain pre {
  color: $highlight-plain-foreground;
}

.gist table {
  width: auto;

  td { border: none; }
}

pre {

  .comment { color: $highlight-comment; }

  .variable
  .attribute
  .tag
  .regexp
  .ruby .constant
  .xml .tag .title
  .xml .pi
  .xml .doctype
  .html .doctype
  .css .id
  .css .class
  .css .pseudo {
    color: $highlight-red;
  }

  .number
  .preprocessor
  .built_in
  .literal
  .params
  .constant
  .command {
    color: $highlight-orange;
  }

  .ruby .class .title
  .css .rules .attribute
  .string
  .value
  .inheritance
  .header
  .ruby .symbol
  .xml .cdata
  .special
  .number
  .formula {
    color: $highlight-green;
  }

  .title
  .css .hexcolor {
    color: $highlight-aqua;
  }

  .function
  .python .decorator
  .python .title
  .ruby .function .title
  .ruby .title .keyword
  .perl .sub
  .javascript .title
  .coffeescript .title {
    color: $highlight-blue;
  }

  .keyword
  .javascript .function {
    color: $highlight-purple;
  }

}

pre .line.output {

  .comment
  .variable
  .attribute
  .tag
  .regexp
  .ruby .constant
  .xml .tag .title
  .xml .pi
  .xml .doctype
  .html .doctype
  .css .id
  .css .class
  .css .pseudo 
  .number
  .preprocessor
  .built_in
  .literal
  .params
  .constant
  .command
  .ruby .class .title
  .css .rules .attribute
  .string
  .value
  .inheritance
  .header
  .ruby .symbol
  .xml .cdata
  .special
  .number
  .formula
  .title
  .css .hexcolor
  .function
  .python .decorator
  .python .title
  .ruby .function .title
  .ruby .title .keyword
  .perl .sub
  .javascript .title
  .coffeescript .title
  .keyword
  .javascript .function {
    color: $highlight-output-color;
  }

}
